card properties
  - title
    - title image
  - supporting text
  - read more
  - share
  - post id

  for each card in recent cards
    create html elements that contain card markup
        create the div containing descriptors about the card
        create the div containing the title - render article title
        create the div containing supporting text - render supporting text
        create the actions div with a read more link
        create the menu div with a share icon
        add post id to card id so post id can be used
        to grab the appropriate card
